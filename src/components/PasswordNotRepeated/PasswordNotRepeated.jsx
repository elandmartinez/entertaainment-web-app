import "../../styles/PasswordNotRepeated.css";

const PasswordNotRepeated = () => {
    return (
        <div className="password-not-repeated">
            <p>Make sure you wrote your password the same in both parameters</p>
        </div>
    )
}

export default PasswordNotRepeated;